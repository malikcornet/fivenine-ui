import { {{pascal slug}} } from '@fivenine-collective/react';

export default function {{pascal slug}}{{pascal variant}}() {
  return <{{pascal slug}}>{{name}} / {{variantTitle variant}}</{{pascal slug}}>;
}
